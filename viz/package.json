{
  "name": "tapestry-viz",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "copy-data": "node scripts/copy-data.mjs",
    "predev": "node scripts/copy-data.mjs",
    "prebuild": "node scripts/copy-data.mjs",
    "dev": "vite",
    "dev:local": "VITE_BASE=/ vite --port 5180 --strictPort",
    "build": "vite build",
    "preview": "vite preview",
    "serve:prod": "VITE_BASE=/ vite build && VITE_BASE=/ vite preview --port 5181 --strictPort"
  },
  "dependencies": {
    "openseadragon": "^4.1.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
